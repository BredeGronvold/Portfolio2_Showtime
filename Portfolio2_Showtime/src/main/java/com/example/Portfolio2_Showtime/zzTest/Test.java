package com.example.Portfolio2_Showtime.zzTest;

import com.example.Portfolio2_Showtime.item.Item;

import java.io.UTFDataFormatException;
import java.net.MalformedURLException;
import java.net.URL;
import java.util.ArrayList;
import java.util.Arrays;

public class Test {
    /*public static void main(String[] args) throws MalformedURLException {
        ArrayList<Item> items = new ArrayList<>(Arrays.asList(
                new Item(0, "a", 22.4, "de", "de"),
                new Item(1, "q", 22.4, "de", "de"),
                new Item(2, "f", 22.4, "de", "de"),
                new Item(3, "e", 22.4, "de", "de"),
                new Item(4, "a", 22.4, "de", "de"),
                new Item(5, "b", 22.4, "de", "de"),
                new Item(6, "x", 22.4, "de", "de"),
                new Item(7, "j", 22.4, "de", "de"),
                new Item(8, "l", 22.4, "de", "de"),
                new Item(9, "g", 22.4, "de", "de")
        ));
        ArrayList<Item> returnList = new ArrayList<>();

        Item[] list = new Item[items.size()];
        for(int i = 0; i<items.size();i++){
            list[i]=items.get(i);
        }

        for (int i = 0; i < list.length; i++) {
            for (int j = i + 1; j < list.length; j++) {
                if(list[i].getName().compareTo(list[j].getName())>0){
                    Item it = list[i];
                    list[i]=list[j];
                    list[j]=it;
                }
            }
        }

        for(int i = 0; i<items.size();i++){
            for(int j = i+1)
        }

        for(Item element : list){
            returnList.add(element);
        }

        for(Item it : returnList){
            System.out.println(it.getName());
        }

    }*/
}
